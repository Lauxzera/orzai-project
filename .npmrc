# Note: script-shell=powershell.exe was removed — it caused npm install to fail
# on Linux (Vercel) because PowerShell does not exist there.
# Scripts that require PowerShell (ops:backup, ops:restore, ops:portable) must
# be run manually on Windows only.
